       section data
       section data
       section code
       global add
       type add func
 add:
       push rbp
       mov rbp, rsp
       sub rsp, 24
       push rbx
 add_block0:
 add_block1:
       mov [rbp, -4], edi
       mov [rbp, -8], esi
       mov eax, [rbp, -4]
       mov ebx, [rbp, -8]
       add eax, ebx
 add_epilog:
       pop rbx
       add rsp, 24
       pop rbp
       ret
