//extern float cos(float);
// extern int ftoi(float);
//extern float itof(int);
//extern float sin(float);
//extern float isqrt(float);
#include "a.h"
#include "b.h"
#include "c.h"

int main() {
    int t = i32 + u32 + l32;
    int alpha = 3;
    int beta = 7;
    int theta = 3;
    float ans = test(alpha, beta, theta);
    ans += t;
    return ans;
}
